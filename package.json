{
  "name": "sports-meet",
  "version": "1.0.0",
  "description": "Sports Meet - Results Management System (Vercel + Supabase)",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
